import { defineMessages } from 'react-intl';

export const scope = 'app.components.AvailableFunds';

export default defineMessages({
  availableFunds: {
    id: `${scope}.available.funds`,
    defaultMessage: 'Available Funds',
  },
});
