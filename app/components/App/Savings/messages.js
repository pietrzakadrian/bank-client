import { defineMessages } from 'react-intl';

export const scope = 'app.components.Savings';

export default defineMessages({
  savings: {
    id: `${scope}.savings`,
    defaultMessage: 'Savings',
  },
});
