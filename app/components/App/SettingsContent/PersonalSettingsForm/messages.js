import { defineMessages } from 'react-intl';

export const scope = 'app.components.PersonalSettingsForm';

export default defineMessages({
  save: {
    id: `${scope}.save`,
    defaultMessage: 'Save data',
  },
});
