/*
 * ForgotPasswordAction Messages
 *
 * This contains all the text for the ForgotPasswordAction component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ForgotPasswordAction';

export default defineMessages({
  action: {
    id: `${scope}.action`,
    defaultMessage: 'Remind password',
  },
});
