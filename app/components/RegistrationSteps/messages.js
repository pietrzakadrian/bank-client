/*
 * RegistrationSteps Messages
 *
 * This contains all the text for the RegistrationSteps component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RegistrationSteps';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RegistrationSteps component!',
  },
});
