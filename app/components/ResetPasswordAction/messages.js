/*
 * ResetPasswordAction Messages
 *
 * This contains all the text for the ResetPasswordAction component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ResetPasswordAction';

export default defineMessages({
  action: {
    id: `${scope}.action`,
    defaultMessage: 'Reset password',
  },
});
